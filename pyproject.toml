[project]
name = "rulebot"
version = "0.1.0"
description = "Slack rule-answering bot prototype"
requires-python = ">=3.11"
dependencies = [
  "slack-bolt>=1.18,<2",
]

[tool.setuptools.packages.find]
where = ["src"]

